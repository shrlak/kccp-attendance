# SSH keys — never commit these
keys/
*.key
*.pem
*.key.pub

# Live data files
data/
attendance-data.json
audit-log.json
config.json
events-data.json
pending-registrations.json
server.log
watch-deploy.log
logs/

# Node
node_modules/
npm-debug.log*
package.json
package-lock.json

# Python (scripts/backup/prune-retention.py)
__pycache__/
*.pyc

# Dev tools
tools/

# OS
.DS_Store
Thumbs.db

# Misc
*.txt
.claude/
docs/*
!docs/manual/
.superpowers/

# React re-platform app (web/) — keep its package manifests tracked
# (root patterns above ignore package.json / package-lock.json globally)
!web/package.json
!web/package-lock.json
