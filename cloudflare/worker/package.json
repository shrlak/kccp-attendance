{
  "name": "kccp-attendance-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply kccp-attendance --local",
    "db:migrate:remote": "wrangler d1 migrations apply kccp-attendance --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.0",
    "@cloudflare/workers-types": "^4.20250109.0",
    "@types/node": "^22.10.0",
    "typescript": "~5.7.0",
    "vitest": "~3.0.5",
    "wrangler": "^3.99.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
